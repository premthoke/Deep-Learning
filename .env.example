# Copy this file to ".env" (or export directly in your shell) and set your
# own OpenWeatherMap API key. The app already has a working default key
# baked in for this experiment, but for any real / long-term use you should
# use your own key via this environment variable instead of hard-coding it.

